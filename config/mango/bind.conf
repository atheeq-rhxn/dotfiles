# reload config
bind=SUPER,r,reload_config

# switch window focus
bind=SUPER,h,focusdir,left
bind=SUPER,l,focusdir,right
bind=SUPER,k,focusdir,up
bind=SUPER,j,focusdir,down

# swap window
bind=SUPER+SHIFT,k,exchange_client,up
bind=SUPER+SHIFT,j,exchange_client,down
bind=SUPER+SHIFT,h,exchange_client,left
bind=SUPER+SHIFT,l,exchange_client,right

# Gesturebind
gesturebind=none,right,3,focusdir,left
gesturebind=none,left,3,focusdir,right
gesturebind=none,up,3,focusdir,down
gesturebind=none,down,3,focusdir,up
gesturebind=none,left,4,viewtoright_have_client
gesturebind=none,right,4,viewtoleft_have_client
gesturebind=none,up,4,toggleoverview,1
gesturebind=none,down,4,toggleoverview,1

# brightness, volume & media
bind=none,XF86AudioRaiseVolume,spawn,qs -c noctalia-shell ipc call volume increase
bind=none,XF86AudioLowerVolume,spawn,qs -c noctalia-shell ipc call volume decrease 

bind=none,XF86AudioMute,spawn,qs -c noctalia-shell ipc call volume muteOutput 
bind=none,XF86AudioMicMute,spawn,qs -c noctalia-shell ipc call volume muteInput

bind=none,XF86AudioPlay,spawn,qs -c noctalia-shell ipc call media playPause
bind=none,XF86AudioStop,spawn,qs -c noctalia-shell ipc call media pause
bind=none,XF86AudioPrev,spawn,qs -c noctalia-shell ipc call media previous
bind=none,XF86AudioNext,spawn,qs -c noctalia-shell ipc call media next

bind=none,XF86MonBrightnessUp,spawn,qs -c noctalia-shell ipc call brightness increase
bind=none,XF86MonBrightnessDown,spawn,qs -c noctalia-shell ipc call brightness decrease

# screen shot and scren capture
bind=none,Print,spawn_shell,FILE=~/Pictures/Screenshots/$(date +\%Y\%m\%d\%H\%M\%S).png && grim -g "$(slurp)" "$FILE" && wl-copy < "$FILE" && notify-send "Screenshot taken" "$FILE"
bind=SHIFT,Print,spawn,qs -c noctalia-shell ipc call screenRecorder toggle

### ---------------------------------- ###

# menu and terminal
bind=Alt,space,spawn,$HOME/.cargo/bin/walker
bind=Alt,t,spawn,ghostty

# exit super+m
bind=SUPER,e,quit
# killclient
bind=SUPER,q,killclient,

# switch window focus
bind=SUPER,Tab,focuslast


# movewin
bind=CTRL+SHIFT,k,movewin,+0,-50
bind=CTRL+SHIFT,j,movewin,+0,+50
bind=CTRL+SHIFT,h,movewin,-50,+0
bind=CTRL+SHIFT,l,movewin,+50,+0

# resizewin
bind=CTRL+ALT,k,resizewin,+0,-50
bind=CTRL+ALT,j,resizewin,+0,+50
bind=CTRL+ALT,h,resizewin,-50,+0
bind=CTRL+ALT,l,resizewin,+50,+0

# switch window status
bind=SUPER,o,toggleoverview,
bind=SUPER,backslash,togglefloating,
bind=SUPER+SHIFT,f,togglefullscreen,
bind=ALT,m,minimized,
bind=ALT+SHIFT,m,restore_minimized

# scroller layout
bind=SUPER,f,set_proportion,1.0
bind=SUPER,x,switch_proportion_preset,

# tile layout
bind=SUPER,d,incnmaster,1
bind=SUPER,i,incnmaster,-1
bind=ALT,s,zoom,

# switch layout
bind=SUPER,t,setlayout,tile
bind=SUPER,s,setlayout,scroller
bind=SUPER,v,setlayout,vertical_scroller
tbind=SUPER,n,switch_layout

# tag switch
# bind=SUPER,Left,viewtoleft,0
# bind=CTRL,Left,viewtoleft_have_client,0
# bind=SUPER,Right,viewtoright,0
# bind=CTRL,Right,viewtoright_have_client,0
# bind=CTRL+SUPER,Left,tagtoleft,0
# bind=CTRL+SUPER,Right,tagtoright,0

bind=Super,1,view,1,0
bind=Super,2,view,2,0
bind=Super,3,view,3,0
bind=Super,4,view,4,0
bind=Super,5,view,5,0
bind=Super,6,view,6,0
bind=Super,7,view,7,0
bind=Super,8,view,8,0
bind=Super,9,view,9,0

bind=Super+Ctrl,1,tag,1,0
bind=Super+Ctrl,2,tag,2,0
bind=Super+Ctrl,3,tag,3,0
bind=Super+Ctrl,4,tag,4,0
bind=Super+Ctrl,5,tag,5,0
bind=Super+Ctrl,6,tag,6,0
bind=Super+Ctrl,7,tag,7,0
bind=Super+Ctrl,8,tag,8,0
bind=Super+Ctrl,9,tag,9,0

# bind=ctrl+Super,1,toggletag,1
# bind=ctrl+Super,2,toggletag,2
# bind=ctrl+Super,3,toggletag,3
# bind=ctrl+Super,4,toggletag,4
# bind=ctrl+Super,5,toggletag,5
# bind=ctrl+Super,6,toggletag,6
# bind=ctrl+Super,7,toggletag,7
# bind=ctrl+Super,8,toggletag,8
# bind=ctrl+Super,9,toggletag,9

# bind=Super,1,toggleview,1
# bind=Super,2,toggleview,2
# bind=Super,3,toggleview,3
# bind=Super,4,toggleview,4
# bind=Super,5,toggleview,5
# bind=Super,6,toggleview,6
# bind=Super,7,toggleview,7
# bind=Super,8,toggleview,8
# bind=Super,9,toggleview,9

# monitor switch
# bind=alt+shift,Left,focusmon,left
# bind=alt+shift,Right,focusmon,right
# bind=alt+shift,Up,focusmon,up
# bind=alt+shift,Down,focusmon,down
# bind=SUPER+Alt,Left,tagmon,left
# bind=SUPER+Alt,Right,tagmon,right
# bind=SUPER+Alt,Up,tagmon,up
# bind=SUPER+Alt,Down,tagmon,down

# gaps
# bind=ALT+SHIFT,X,incgaps,1
# bind=ALT+SHIFT,Z,incgaps,-1
# bind=ALT+SHIFT,R,togglegaps


#custom app bind
bind=ALT,B,spawn,flatpak run app.zen_browser.zen
bind=ALT,y,spawn,ghostty -e $HOME/.cargo/bin/yazi
bind=ALT,d,spawn,flatpak run com.discordapp.Discord
